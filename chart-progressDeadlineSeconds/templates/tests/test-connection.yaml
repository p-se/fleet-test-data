apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-progressDeadlineSeconds.fullname" . }}-test-connection"
  labels:
    {{- include "chart-progressDeadlineSeconds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-progressDeadlineSeconds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
