apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reproducer.fullname" . }}-test-connection"
  labels:
    {{- include "reproducer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reproducer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
