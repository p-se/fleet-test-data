apiVersion: v1
kind: Service
metadata:
  name: {{ include "reproducer.fullname" . }}
  labels:
    {{- include "reproducer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reproducer.selectorLabels" . | nindent 4 }}
